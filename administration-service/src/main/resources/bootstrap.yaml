spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: administration-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    plataform: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${POSTGRES_DB_HOST}:5432/${POSTGRES_DB_ADMINISTRATION}?useSSL=false
    username: postgres
    password: adm123
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081